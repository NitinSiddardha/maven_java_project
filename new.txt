->jenkins pipeline script -
pipeline {
    agent any
    tools { maven 'MAVEN3' }
    stages {
        stage('clean') {
            steps {
                git 'https://github.com/NitinSiddardha/maven_webapp.git'
                bat 'mvn clean'
            }
        }
        stage('install') {
            steps {
                bat 'mvn install'
            }
        }
    }
}
->kubernets using minikube -
minikube start
kubectl create deployment mynginx --image=nginx
kubectl expose deployment mynginx --port=80 --target-port=80
kubectl port-forward service/mynginx 8090:80
#kubectl scale deployment mynginx --replicas=4, kubectl get pods, kubectl describe pods
